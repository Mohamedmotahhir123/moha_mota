buildozer init
[app]
# (خل التفاحة، اسم التطبيق)
title = Smiley Game
# (اسم حزمة التطبيق)
package.name = smiley_game_by_moha_mota
# (نطاق الحزمة)
package.domain = org.moha_mota
# (إصدار التطبيق)
version = 1.0
# (المكتبات المطلوبة)
requirements = kivy, python3
# (ملفات البيانات المضافة)
source.include_exts = sssm.py
